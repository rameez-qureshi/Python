a = { 1, 2, 3, 4, 5, 1}       # set does not contain repeated elements (In results)
print(a)

b = {}                       # This syntax create empty dictionary not an empty set
print(type(b))

c = set()                       # Method for empty set
print(type(c))


# Set is unordered
# Set is unindexed
# Set element is unchangeable
# Set can't contain duplicate elements